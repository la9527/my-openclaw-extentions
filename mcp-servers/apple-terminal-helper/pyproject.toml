[project]
name = "apple-terminal-helper"
version = "0.1.0"
description = "macOS Terminal.app IPC helper for Apple Photos operations"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["apple_terminal_helper"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
